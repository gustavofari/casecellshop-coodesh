{
  "name": "case-cell-shop",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.18.2",
  "description": "Sistema de checkout resiliente para e-commerce de capinhas de celular.",
  "scripts": {
    "dev": "concurrently \"pnpm --dir backend start:dev\" \"pnpm --dir frontend dev\"",
    "install:all": "pnpm --dir backend install && pnpm --dir frontend install",
    "build:all": "pnpm --dir backend build && pnpm --dir frontend build",
    "test:e2e": "pnpm --dir frontend cypress:run"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
